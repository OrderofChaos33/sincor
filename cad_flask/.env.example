# Generate one: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-me
DATABASE_URL=sqlite:///cad.db
# Optional email settings (no-op by default)
MAIL_SENDER=no-reply@clintondetailing.com
