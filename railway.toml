[build]
builder = "nixpacks"
<<<<<<< Updated upstream

[deploy]
startCommand = "python simple.py"
=======
buildCommand = "pip install -r requirements.txt"

[deploy]
startCommand = "gunicorn --bind 0.0.0.0:$PORT app:app"
healthcheckPath = "/healthz"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[[services]]
name = "sincor-web"
source = "."
variables = { FLASK_ENV = "production" }

[[services]]
name = "sincor-scheduler"
source = "./services/scheduler"
startCommand = "python sincor_scheduler.py --redis-url $REDIS_URL"
variables = { SCHEDULER_ENABLED = "true" }

[[services]]
name = "sincor-marketing-engine"
source = "./services/marketing_workflow" 
startCommand = "python integrated_marketing_engine.py"
variables = { ENABLE_CONTENT_GENERATION = "true" }

[environments.production]
variables = {
  FLASK_ENV = "production",
  FLASK_DEBUG = "false",
  PORT = "8000",
  HOST = "0.0.0.0"
}

[environments.staging]
variables = {
  FLASK_ENV = "staging", 
  FLASK_DEBUG = "true"
}
>>>>>>> Stashed changes
